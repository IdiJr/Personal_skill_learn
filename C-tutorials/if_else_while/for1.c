#include <stdio.h>

int main(void)
{
	for (int i = 0; i <= 10; i++)  /*for loop is used to do repeated tasks */
	{
		printf("Hello, my friend\n");
	}
	for (int j = 0; j <= 20; j++)
	{
		printf("Number %d\t", j);
	}
	return (0);
}
